type User {
    id: String!
    username: String!
    email: String
    verified: Boolean!
    token: String
    img: String
    lastLoginAt: Date
}

fragment User on User {
    id
    username
    email
    verified
    img
    lastLoginAt
}
