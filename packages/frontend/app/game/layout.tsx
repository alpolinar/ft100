const GameRootLayout = ({ children }: React.PropsWithChildren) => {
    return (
        <div>
            <p>game root layout</p>
            {children}
        </div>
    );
};

export default GameRootLayout;
